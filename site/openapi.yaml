openapi: 3.1.0
info:
  title: Binky
  version: "0.0.0"
  description: |
    Binky is a native macOS desktop application for organizing Downloads. There is no public HTTP API on binkyfiles.com.
    This document exists only for machine-readable discovery (RFC 9727 service-desc).
paths: {}
